const ANIMATION = {
    EXPAND_LARGE : 3,
    FADE_OUT : "FADE_OUT",
    EXPAND : "EXPAND"
}

Object.freeze(ANIMATION);

const DEFAULT = {
    TITLE : "DEFAULT TITLE",
    NAV_ITEM : "DEFAULT NAV"
}

const CONFIG = {
    SCROLL_SENCIBILITY : 150
}

